# lpin-android-sdk-space-pixel-matching

픽셀 매칭 공간 인증

- OpenCV native SDK는 `third_party`의 중립 vendor 경로를 사용합니다.
- 이 모듈은 Gradle에서 전달받은 `LPIN_OPENCV_DIR` CMake 인자로 OpenCV를 찾고, wrapper 표면은 `space/authenticator`에 통합되어 있습니다.
